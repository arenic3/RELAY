<!doctype html>
<html>
  <head>
    <title><%=pageName%></title>
    <link rel="stylesheet"  type="text/css" href="/RELAY/styles.css" /><!--CSS-->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script><!--Include JQUERY-->
  </head>
  <body class="staffPage">
    <!--Header with Logo home button and current user info-->
    <h1><a class="staffHome" href="/RELAY/staff-home"><%=pageName%></a> <span class="welcome"> Welcome back <%=userInfo[0].name%></span></h1>
    <div class="container">
      <nav>
        <p><a href="/RELAY/about">About</a></p>
        <p><a href="/RELAY/staff-communication">Start chatting</a></p>
      </nav>
        <!--Sign out button-->
      <div class="signout">
        <button id="signoutButton">Sign Out</button>
      </div>
    </div>

    <!--Sign out functionality-->
    <script>
      $(document).ready(function() {
      //Sign out button event listener
      $('#signoutButton').on('click', function() {
          // Call the sign-out function
          signOut();
      });
    });

    //Sign-out function
    function signOut() {
      $.ajax({
          url: '/RELAY/sign-out',
          type: 'GET',
          success: function(response) {
              //Redirect
              window.location.href = '/RELAY/';
          },
          error: function(xhr, status, error) {
              console.error('Error signing out:', error);
          }
      });
    }
    </script>
  </body>
</html>